export interface UserData {
	createdAt: string;
	deleted: boolean;
	email: string;
	id: string;
	name: string;
	photo: string;
	session?: unknown;
	suspended: boolean;
	updatedAt: string;
	verified: boolean;
}
