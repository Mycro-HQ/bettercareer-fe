import React from 'react';
import EmptyStateContainer from './empty-state-container';

const Experience = () => {
	return (
		<div>
			<EmptyStateContainer
				text="Add your work experience: Include past and present positions to build your resume"
				buttonText="Add Experience"
				onClick={() => {}}
			/>
		</div>
	);
};

export default Experience;
