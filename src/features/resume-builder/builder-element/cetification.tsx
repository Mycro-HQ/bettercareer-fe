import React from 'react';
import EmptyStateContainer from './empty-state-container';

const Cetification = () => {
	return (
		<div>
			<EmptyStateContainer
				text="Add your certifications to demonstrate relevant skills and knowledge."
				buttonText="Add Certificate"
				onClick={() => {}}
			/>
		</div>
	);
};

export default Cetification;
