import React from 'react';
import EmptyStateContainer from './empty-state-container';

const Education = () => {
	return (
		<div>
			<EmptyStateContainer
				text="Add your education: Simply fill in the details to showcase your academic background."
				buttonText="Add Education"
				onClick={() => {}}
			/>
		</div>
	);
};

export default Education;
